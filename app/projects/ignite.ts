export interface IgniteContent {
  title: string;
  description: string;
  // Add any other fields you need
}

export const igniteContent: IgniteContent = {
  title: "Ignite",
  description: "Breaking barriers for entry into STEM education",
  // Add more content fields as needed
}
